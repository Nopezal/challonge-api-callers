First and foremost, install the latest Python 3 version! This is available over at https://www.python.org/
Make sure to update the "update-ras.bat" file with your credentials, that is username & API key, and also change the domain (and subdomain if necessary)!
Also make sure to run the "install.bat" file first!